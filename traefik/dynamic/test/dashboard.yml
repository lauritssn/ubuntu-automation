http:
  routers:
    dashboard-test:
      rule: "Host(`traefik.localtest`)"
      service: "api@internal"
      entryPoints:
        - "traefik"
      priority: 100
      middlewares:
        - "test-headers@file" 