services:
  traefik:
    image: docker.io/traefik:v3.5.0
    container_name: traefik-dev
    restart: unless-stopped
    user: "0:0"
    ports:
      - 80:80
      - 8080:8080
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    volumes:
      - /run/podman/podman.sock:/var/run/docker.sock:ro
      - ./traefik/dev/traefik.yml:/etc/traefik/traefik.yml:ro,Z
      - ./traefik/dev/dynamic:/etc/traefik/sites/config:ro,Z
      - ./data:/data:Z
      - ./logs:/var/log/traefik:Z
      - ./plugins-storage:/plugins-storage:Z
    networks:
      - traefik
    healthcheck:
      test: ["CMD", "traefik", "healthcheck", "--ping"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
  httpbin:
    image: docker.io/kennethreitz/httpbin:latest
    container_name: httpbin-dev
    restart: unless-stopped
    networks:
      - traefik

  whoami:
    image: docker.io/traefik/whoami:latest
    container_name: whoami-dev
    restart: unless-stopped
    networks:
      - traefik

networks:
  traefik:
    name: traefik
    driver: bridge
    external: true
