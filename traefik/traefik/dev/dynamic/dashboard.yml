http:
  routers:
    dashboard-dev:
      rule: "Host(`traefik.localdev`)"
      service: "api@internal"
      entryPoints:
        - "traefik"
      priority: 100
      middlewares:
        - "dev-headers@file" 
        - "coraza-waf-dev@file"